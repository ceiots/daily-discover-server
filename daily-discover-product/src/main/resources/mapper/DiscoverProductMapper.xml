<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dailydiscover.mapper.DiscoverProductMapper">
    <!-- 基础结果映射 -->
    <resultMap id="BaseResultMap" type="com.dailydiscover.model.ProductEntity">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="description" property="description" />
        <result column="price" property="price" />
        <result column="image_url" property="imageUrl" />
        <result column="category_id" property="categoryId" />
        <result column="is_active" property="isActive" />
        <result column="view_count" property="viewCount" />
        <result column="like_count" property="likeCount" />
        <result column="favorite_count" property="favoriteCount" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>
    
    <!-- 查询今日推荐商品 -->
    <select id="findTodayProducts" resultMap="BaseResultMap">
        SELECT p.* FROM products p
        INNER JOIN daily_recommend_products drp ON p.id = drp.product_id
        WHERE drp.recommend_date = CURDATE() AND p.is_active = TRUE
        ORDER BY drp.sort_order ASC, p.created_at DESC
    </select>
    
    <!-- 查询昨日推荐商品 -->
    <select id="findYesterdayProducts" resultMap="BaseResultMap">
        SELECT p.* FROM products p
        INNER JOIN daily_recommend_products drp ON p.id = drp.product_id
        WHERE drp.recommend_date = DATE_SUB(CURDATE(), INTERVAL 1 DAY) AND p.is_active = TRUE
        ORDER BY drp.sort_order ASC, p.created_at DESC
    </select>
    
    <!-- 根据主题查询商品 -->
    <select id="findProductsByTheme" resultMap="BaseResultMap">
        SELECT p.* FROM products p
        INNER JOIN daily_recommend_products drp ON p.id = drp.product_id
        WHERE drp.theme_id = #{themeId} AND drp.recommend_date = #{date} AND p.is_active = TRUE
        ORDER BY drp.sort_order ASC, p.created_at DESC
    </select>
    
    <!-- 查询所有活跃商品 -->
    <select id="findAllActiveProducts" resultMap="BaseResultMap">
        SELECT * FROM products 
        WHERE is_active = TRUE
        ORDER BY created_at DESC
    </select>
    
    <!-- 根据分类查询商品 -->
    <select id="findProductsByCategory" resultMap="BaseResultMap">
        SELECT * FROM products 
        WHERE category_id = #{categoryId} AND is_active = TRUE
        ORDER BY created_at DESC
    </select>
    
    <!-- 根据ID查询商品 -->
    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM products WHERE id = #{id}
    </select>
    
    <!-- 插入商品 -->
    <insert id="insert" parameterType="com.dailydiscover.model.ProductEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO products (name, description, price, image_url, category_id, is_active, view_count, like_count, favorite_count, created_at, updated_at)
        VALUES (#{name}, #{description}, #{price}, #{imageUrl}, #{categoryId}, #{isActive}, #{viewCount}, #{likeCount}, #{favoriteCount}, #{createdAt}, #{updatedAt})
    </insert>
    
    <!-- 更新商品 -->
    <update id="update" parameterType="com.dailydiscover.model.ProductEntity">
        UPDATE products 
        SET name = #{name}, 
            description = #{description}, 
            price = #{price}, 
            image_url = #{imageUrl}, 
            category_id = #{categoryId}, 
            is_active = #{isActive}, 
            view_count = #{viewCount}, 
            like_count = #{likeCount}, 
            favorite_count = #{favoriteCount}, 
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>
    
    <!-- 更新商品浏览次数 -->
    <update id="updateViewCount">
        UPDATE products SET view_count = view_count + 1 WHERE id = #{id}
    </update>
    
    <!-- 更新商品点赞次数 -->
    <update id="updateLikeCount">
        UPDATE products SET like_count = like_count + 1 WHERE id = #{id}
    </update>
    
    <!-- 更新商品收藏次数 -->
    <update id="updateFavoriteCount">
        UPDATE products SET favorite_count = favorite_count + 1 WHERE id = #{id}
    </update>
    
    <!-- 删除商品 -->
    <delete id="delete">
        DELETE FROM products WHERE id = #{id}
    </delete>
    
    <!-- 批量插入商品 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO products (name, description, price, image_url, category_id, is_active, view_count, like_count, favorite_count, created_at, updated_at)
        VALUES 
        <foreach collection="products" item="product" separator=",">
            (#{product.name}, #{product.description}, #{product.price}, #{product.imageUrl}, #{product.categoryId}, #{product.isActive}, #{product.viewCount}, #{product.likeCount}, #{product.favoriteCount}, #{product.createdAt}, #{product.updatedAt})
        </foreach>
    </insert>
</mapper>