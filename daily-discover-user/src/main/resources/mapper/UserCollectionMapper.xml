<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dailydiscover.user.mapper.UserCollectionMapper">

    <resultMap id="BaseResultMap" type="com.dailydiscover.user.entity.UserCollection">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="item_type" property="itemType" jdbcType="VARCHAR"/>
        <result column="item_id" property="itemId" jdbcType="VARCHAR"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
        <result column="image_url" property="imageUrl" jdbcType="VARCHAR"/>
        <result column="price" property="price" jdbcType="DECIMAL"/>
        <result column="original_price" property="originalPrice" jdbcType="DECIMAL"/>
        <result column="collected_at" property="collectedAt" jdbcType="TIMESTAMP"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 删除用户的所有收藏 -->
    <delete id="deleteByUserId">
        DELETE FROM user_collections WHERE user_id = #{userId}
    </delete>

    <!-- 统计用户收藏数量 -->
    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM user_collections WHERE user_id = #{userId}
    </select>

    <!-- 检查用户是否已收藏某个内容 -->
    <select id="selectByUserAndItem" resultMap="BaseResultMap">
        SELECT * FROM user_collections 
        WHERE user_id = #{userId} AND item_type = #{itemType} AND item_id = #{itemId}
    </select>

</mapper>