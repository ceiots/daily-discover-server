<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserMapper">
    <select id="findByPhoneNumber" resultType="com.example.model.User">
        SELECT * FROM users WHERE phone_number = #{phoneNumber}
    </select>

    <insert id="registerUser" parameterType="com.example.model.User">
        INSERT INTO users (phone_number, password, nickname, registration_time)
        VALUES (#{phoneNumber}, #{password}, #{nickname}, #{registrationTime})
    </insert>

    <update id="updatePassword">
        UPDATE users SET password = #{password} WHERE phone_number = #{phoneNumber}
    </update>
</mapper> 