<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.OrderMapper">
    <update id="updateOrderStatus" parameterType="com.example.model.Order">
        UPDATE orders 
        SET status = #{status},
            payment_method = #{paymentMethod},
            payment_amount = #{paymentAmount},
            payment_time = #{paymentTime}
        WHERE id = #{id}
    </update>

    <update id="cancelOrder">
        UPDATE orders SET status = 2 WHERE id = #{orderId}
    </update>

    <select id="getUserOrders" resultType="com.example.model.Order">
        SELECT * FROM orders 
        WHERE user_id = #{userId}
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="getAllOrders" resultType="com.example.model.Order">
        SELECT * FROM orders ORDER BY create_time DESC
    </select>

    <insert id="createOrder" parameterType="com.example.model.Order" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (user_id, status, create_time)
        VALUES (#{userId}, 0, #{createTime})
    </insert>
</mapper> 