# 通用模块配置
# 作为公共库被其他服务引入时生效

spring:
  # 禁用Web服务器启动
  main:
    web-application-type: none

# 日志配置
logging:
  level:
    com.dailydiscover.common: INFO

# 自定义配置
daily-discover:
  common:
    mode: library